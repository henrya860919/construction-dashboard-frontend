---
description: 前端技術棧與專案結構總覽
alwaysApply: true
---

# Construction Dashboard - Frontend

## 技術棧

- **框架**: Vue 3 + Vite
- **UI 元件**: shadcn-vue
- **圖表**: vue-echarts（工程儀表板圖表）
- **狀態管理**: Pinia
- **部署**: Vercel（CI/CD 由 GitHub 觸發）

## 目錄結構約定

- `src/components/` - 可重用元件（依功能分子目錄）
- `src/views/` 或 `src/pages/` - 頁面級元件
- `src/stores/` - Pinia stores
- `src/api/` 或 `src/services/` - API 呼叫封裝
- `src/composables/` - 共用 composables
- `src/assets/` - 靜態資源

## 環境變數

- 僅使用 `VITE_` 前綴（Vite 會暴露給 client）
- API base URL: `VITE_API_URL`（dev / preview / production 各環境不同）
- 勿在前端存放 secret 或 API key

## 命名

- 元件檔名: PascalCase 或 kebab-case（專案內擇一統一）
- 元件名: PascalCase
- 變數/函式: camelCase
- 常數: UPPER_SNAKE_CASE 或 camelCase
