---
description: 前端技術棧與專案結構總覽
alwaysApply: true
---

# Construction Dashboard - Frontend

## 技術棧

- **框架**: Vue 3 + Vite
- **UI 元件**: shadcn-vue (new-york style)
- **圖表**: vue-echarts（工程儀表板圖表）
- **狀態管理**: Pinia
- **部署**: Vercel（CI/CD 由 GitHub 觸發）

## 目錄結構

- `src/api/` - 後端 API 呼叫（client、auth、依領域分檔）
- `src/assets/` - 靜態資源、全域 CSS
- `src/components/` - 可重用元件
  - `ui/` - shadcn-vue（用 CLI 加，勿手動改 CVA）
  - `common/` - **跨多個 view／多個領域共用**的自訂元件（StateCard、DataTablePagination、AppHeader、AnimatedNumber 等）
  - `dashboard/`、依領域子目錄 - **僅單一 view 或單一領域使用**的元件（如僅儀表板用的 AlertCard、MonitorCard、ChartSummaryCards）
  - **元件擺放邏輯**：被 2 個以上不同 view 或不同模組使用 → 放 `common/`；僅單一 view／單一領域使用 → 放該領域資料夾（如 `dashboard/`）。勿把跨域共用元件放在 dashboard 等領域夾內。
- `src/composables/` - 共用邏輯（useAuth、usePagination 等）
- `src/constants/` - 路由、API 路徑、選項等常數，避免 magic string
- `src/layouts/` - 版面（DefaultLayout、AuthLayout），router 套用
- `src/lib/` - 工具（`cn`、`format-number` 千分位／金額顯示 等）
- `src/router/` - 路由設定，使用 constants 的 ROUTE_PATH / ROUTE_NAME
- `src/stores/` - Pinia stores
- `src/types/` - 共用 TypeScript 型別（API、auth、領域）
- `src/views/` - 頁面級元件，對應路由
  - **`src/views/mobile/`** - 手機／PWA 現場查驗（路由前綴 `/mobile`）；實作規範見 **`docs/mobile-field-app-implementation-guide.md`**、規則 **`.cursor/rules/mobile-pwa-field-app.mdc`**

## 依賴方向

- views 可 import：components、stores、composables、api、router、constants、types、lib
- components 不直接 call API，透過 props / stores / composables 拿資料
- stores 可 import api、lib、types
- api 只依賴 client、constants、types

## 環境變數

- 僅使用 `VITE_` 前綴；API base URL: `VITE_API_URL`
- 勿在前端存放 secret 或 API key

## 命名

- 元件檔名: PascalCase 或 kebab-case（專案內擇一統一）
- 元件名: PascalCase
- 變數/函式: camelCase
- 常數: UPPER_SNAKE_CASE（constants/ 內）

## 新頁面開發

- **做新頁面或列表／表格頁時，請先讀 `.cursor/rules/ui-ux-principles.mdc`**，依其中版面、按鈕、表格、多選、導航等原則與檢查清單實作；寬表格與水平捲動見 **`table-scroll-layout.mdc`**。
- 同時依情境參考：project-routes.mdc（專案內路徑與側欄）、theme-support.mdc（深淺色）、api-contract.mdc（API 與錯誤）、shadcn-vue-official.mdc（UI 元件）。
- **手機／PWA 現場查驗新功能**：讀 **`docs/mobile-field-app-implementation-guide.md`** 與 **`mobile-pwa-field-app.mdc`**（歷史堆疊、`MobileLayout`、iOS PWA 等）。
